{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ironhell.local/schemas/activations.schema.json",
  "title": "Artifact Activation Definitions",
  "description": "Maps legacy activation IDs to composition-based effect chains. Preserves all original activation names while replacing hardcoded behavior with parameterized actions.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "activations"],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "activations": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/ActivationDefinition" }
    }
  },
  "$defs": {
    "ActivationId": {
      "type": "string",
      "pattern": "^[A-Z][A-Z0-9_]*$"
    },
    "ActivationDefinition": {
      "type": "object",
      "additionalProperties": false,
      "required": ["activation_id", "name", "actions"],
      "properties": {
        "activation_id": {
          "description": "The legacy uppercase activation name used in artifacts.json. Must not be changed.",
          "$ref": "#/$defs/ActivationId"
        },
        "name": {
          "description": "Human-readable display name for UI.",
          "type": "string",
          "minLength": 1
        },
        "description": {
          "description": "Lore-appropriate description shown to the player.",
          "type": "string"
        },
        "actions": {
          "description": "Ordered action references applied when this activation fires. Nested action lists are permitted for composite source configuration.",
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/ActionReference" }
        },
        "migration_status": {
          "description": "Marks an activation whose remaining behavior cannot yet be represented by an activation-eligible canonical action.",
          "type": "string",
          "enum": ["unresolved"]
        },
        "notes": {
          "description": "Implementation notes for developers. Not shown to players.",
          "type": "string"
        }
      }
    },
    "ActionReference": {
      "type": "object",
      "additionalProperties": true,
      "required": ["action_id"],
      "properties": {
        "action_id": { "type": "string", "minLength": 1 },
        "parameters": { "type": "object" }
      }
    }
  }
}
